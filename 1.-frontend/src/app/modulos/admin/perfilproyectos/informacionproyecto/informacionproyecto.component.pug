mat-card.formulario-proyecto
    .titulo-formulario-proyecto
        h1 Formulario del proyecto: {{proyectos.nombre}}
    formularioproyectos([proyecto]="proyectos", [options]="options", *ngIf="options")